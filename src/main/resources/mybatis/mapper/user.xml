<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="user">

	<insert id="userInsert" parameterType="uservo">
	<![CDATA[
		insert into user values(null, #{name}, #{email}, #{password}, #{gender}, now())	
	]]>
	</insert>
	<select id="userGetByEmailAndPassword" parameterType="uservo" resultType="uservo">
	<![CDATA[
		select no, name from user
		where email=#{email} and password=#{password}
	]]>
	</select>
	<select id="userGetByNo" parameterType="java.util.Map" resultType="uservo">
	<![CDATA[
		select no, name, email, password, gender 
		from user
		where no=#{no}
	]]>
	</select>
	<update id="userUpdate" parameterType="uservo">
	<![CDATA[
		update user set
		name=#{name},
		password=#{password},
		gender=#{gender}
		where no=#{no}
	]]>
	</update>
</mapper>
